<Page x:Class="pz_008.ContactInfoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pz_008"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ContactInfoPage">

    <Grid>
        <!--<TextBlock x:Name="tb1" Text="contact info" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>-->

        <StackPanel x:Name="foodInfo">
            <TextBlock Text="{Binding Path=Name}" FontSize="18"/>
            <TextBlock Text="{Binding Path=Phone}" FontSize="18"/>
            <TextBlock Text="{Binding Path=Address}" FontSize="18"/>
            <TextBlock Text="{Binding Path=Description}" FontSize="18"/>
            <TextBlock Text="{Binding Path=Menu}" FontSize="18"/>
        </StackPanel>

        <StackPanel x:Name="schoolInfo"></StackPanel>

        <StackPanel x:Name="pesonInfo"></StackPanel>
    </Grid>
</Page>
